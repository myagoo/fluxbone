/** @jsx React.DOM */

var React = require('react');

var TodoList = React.createClass({
    render: function(){
        return (
            <h1>Hello world</h1>
        );
    }
});

module.exports = TodoList;
